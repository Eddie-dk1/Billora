# Billora Bootstrap Notes

This repository now uses a Next.js App Router baseline with:
- Tailwind CSS v4
- Prisma schema (PostgreSQL)
- Auth.js skeleton
- Vitest unit tests for recurrence engine rules

Legacy Figma prototype has been moved to `legacy/figma-prototype/`.
